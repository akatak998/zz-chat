services:
  app:
    build: .
    tty: true
    ports:
      - 8000:8000
    volumes:
      - .:/app
      - node_modules:/app/node_modules
    depends_on:
      - db
  db:
    image: postgres:16.1
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: zz_chat
      TZ: "Asia/Tokyo"
    volumes:
      - ../zz-chat-db:/var/lib/postgresql/data

volumes:
  node_modules: